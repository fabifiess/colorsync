{
  "name": "ColorSync",
  "version": "0.0.1",
  "description": "Choose a color in the color wheel on the web interface. The color changes on each connected device.",
  "dependencies": {
    "express": "4.3.1",
    "indexof": "0.0.1",
    "socket.io": "^1.0.2"
  }
}
